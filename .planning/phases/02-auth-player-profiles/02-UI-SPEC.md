---
phase: 2
slug: auth-player-profiles
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-02
---

# Phase 2 — UI Design Contract: Auth & Player Profiles

> Visual and interaction contract for Phase 2. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — vanilla JS, no framework |
| Icon library | none — inline emoji and inline SVG only |
| Font (display) | "Titan One" (weight 400), "Bubblegum Sans" (weight 400) — Google Fonts |
| Font (UI panels) | -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif — system stack |

Source: CSS custom properties in `prototypes/penguin-game.html` and `prototypes/hud.css`.

No shadcn gate applies — project is vanilla JS, not React/Next.js/Vite.

---

## Color Tokens

All tokens already declared as CSS custom properties in `prototypes/penguin-game.html :root`. Phase 2 must reference these tokens; no new tokens are introduced.

| Token | Hex | Source |
|-------|-----|--------|
| `--teal` | `#68C7C1` | `:root` in penguin-game.html |
| `--yellow` | `#FACA78` | `:root` |
| `--peach` | `#F57F5B` | `:root` |
| `--red` | `#DD5341` | `:root` |
| `--brown` | `#794A3A` | `:root` |
| `--cream` | `#F5EDD6` | `:root` |
| `--dark` | `#2A1A0E` | `:root` |

Additional surface values in use (not yet tokenised — Phase 2 may reference as literals matching prototype):

| Name | Hex | Role |
|------|-----|------|
| game-bg | `#110822` | Page/game background (deep purple-black) |
| fp-dark | `#0a0a0f` | Dark card surface (post-run, dark mode first-play panel) |
| panel-white | `#ffffff` | Menu panel (gsp-panel) surface |
| accent-purple | `#534AB7` | Tab active indicator, avatar bg, account CTA button, leaderboard "me" highlight |
| action-amber | `#EF9F27` | Attempt dots, primary play button (dark-surface variant) |

---

## Color Contract (60 / 30 / 10)

| Role | Value | Usage | % |
|------|-------|-------|---|
| Dominant surface | `#F5EDD6` (`--cream`) | Auth form panel background (light zone); all new auth forms live on cream surface matching `.fp` title zone | 60% |
| Secondary surface | `#ffffff` | Menu panel (`#gsp-panel`) where Account tab lives | 30% |
| Accent | `#DD5341` (`--red`) | Primary CTA buttons only — "Create account", "Save changes" | 10% |
| Destructive | `rgba(255,100,100,0.4)` border + `#f5a0a0` text | Sign Out button only — never use red fill on destructive; use existing ghost style | — |

Accent (`--red`) is reserved for: the primary CTA button in the auth form, the "Create account" submit button, and the "Play now" CTA. Do not apply `--red` to links, labels, badges, or input focus rings.

Input focus ring: `#534AB7` (accent-purple) — 2px solid, matches existing tab active indicator.

---

## Spacing Scale

Declared values (multiples of 4 only). All values match the rhythm already established in the prototype:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline field padding nudge |
| sm | 8px | Gap between stacked inputs, gap between inline buttons, stat cell gap |
| md | 16px | Panel body padding (`#gsp-body: padding: 16px`), form section spacing |
| lg | 24px | Vertical separation between form groups (label cluster + input cluster) |
| xl | 32px | Top/bottom padding for standalone auth card on large viewports |
| 2xl | 48px | Reserved — not used in this phase |
| 3xl | 64px | Reserved — not used in this phase |

Exceptions:
- Touch targets: all interactive controls minimum 44×44px (SHEL-02, carried from Phase 1). Auth buttons must meet this; use `padding: 13px 14px` minimum (matching existing `.fp-btn-play`).
- Input field height: minimum 44px tall for tap accuracy on mobile. Use `padding: 10px 12px` + `font-size: 16px` (16px prevents iOS zoom on focus).
- Checkbox touch target: wrap label in a block with `min-height: 44px` and `display: flex; align-items: center`.

---

## Typography

Source: extracted from prototype. UI panel typography uses system sans-serif stack; display elements use Google Fonts.

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Body | 13px | 400 | 1.5 | system sans-serif | Field placeholder text, helper copy, nudge lines, panel `<p>` |
| Label | 11px | 500 | 1.4 | system sans-serif | Form field labels, stat keys, eyebrow text (uppercase, letter-spacing 0.06em) |
| Heading | 16px | 500 | 1.2 | system sans-serif | Account tab section titles, panel headings (e.g. "My Account") |
| Display | 19px | 400 | 1.35 | Bubblegum Sans | Auth panel hook line (fp-hook), age declaration banner heading |

Exactly 2 weights: 400 (regular) and 500 (medium). No bold (700) used in UI panels.

Note: `font-size: 16px` on all `<input>` elements is mandatory — prevents iOS Safari automatic zoom on focus.

---

## Screens and Components

### 1. Signup Form (extends existing `.fp-auth-embed`)

Location: embedded in first-play card (`.fp` panel, light/cream surface zone).

Fields (in order):
1. Display name — `type="text"`, `autocomplete="nickname"`, placeholder: "Display name (shown on leaderboard)"
2. Email — `type="email"`, `autocomplete="email"`, placeholder: "Email"
3. Password — `type="password"`, `autocomplete="new-password"`, placeholder: "Password (8+ characters)"
4. Age declaration — checkbox: "I am 18 or older" with inline label. Must be checked to enable submit.

Buttons:
- Primary: "Create account" — full-width, `--red` fill, `--brown` border, Bubblegum Sans 16px, border-radius 50px, box-shadow 4px 4px 0 `--brown`. Matches existing `.fp-btn-play.btn-play` style exactly.
- Secondary: "Sign in instead" — ghost style, `--teal` text, no border, Bubblegum Sans 14px underline. Matches existing `#ov-card[data-zone="title"] .fp-btn-signin`.

Submit disabled state: when age checkbox is unchecked, "Create account" button is `opacity: 0.4` and `pointer-events: none`.

Validation messages: displayed in existing `#auth-status` span, `font-size: 11px`, success `#7dffb0`, error `#f5a0a0`.

### 2. Sign In Form (existing, minor polish)

Fields:
1. Email — `type="email"`, `autocomplete="email"`
2. Password — `type="password"`, `autocomplete="current-password"`

Buttons:
- Primary: "Sign in" — same style as Create account button
- Secondary: "Create account instead" — ghost style

Add below buttons: "Forgot password?" — text link, `font-size: 12px`, `color: var(--teal)`, no underline by default, underline on hover/focus.

### 3. Forgot Password Screen

Triggered by: tapping "Forgot password?" link.

Replaces auth form fields in place (JS swap, no page navigation):

Fields:
1. Email — `type="email"`, `autocomplete="email"`, placeholder: "Your account email"

Buttons:
- Primary: "Send reset link" — full-width, same button style
- Secondary: "Back to sign in" — ghost, `--teal` text

Confirmation state (after submit): hide form, show confirmation message in `#auth-status`:
- Copy: "Reset link sent — check your email."
- Color: `#7dffb0` (existing success color)

### 4. Account Tab (in `#gsp-panel` menu)

Location: `data-pane="account"` inside the existing white menu panel. Panel body `padding: 16px`.

Layout (stacked, gap 16px between groups):

**User identity block:**
- Avatar circle: 34px, `background: #534AB7`, initials (first letter of display name), font-size 12px, weight 500, color `#CECBF6`
- Display name: `font-size: 14px`, weight 500, color `#1a1a1a`
- Age tier badge: inline pill right of display name
  - 18+ competitors: `background: #FAEEDA`, `color: #633806`, text "Competitor", `border-radius: 20px`, `padding: 2px 8px`, `font-size: 11px` — matches existing `.lb-prize-pill`
  - Under-18: `background: rgba(83,74,183,0.12)`, `color: #534AB7`, text "Practice Mode", same sizing

**Account info (read-only in v1):**
- Email address: `font-size: 12px`, `color: #888`
- Display name field: read-only in v1 — show as static text with "Edit" note deferred to v2

**Actions:**
- "Sign out" button — existing `#auth-signout` ghost style: `border: 0.5px solid rgba(255,100,100,0.4)`, `color: #f5a0a0`, `font-size: 11px`, `border-radius: 8px`, `padding: 6px 12px`

Account tab authenticated empty state (user not logged in while Account tab is open):
- Body copy: "Sign in to see your account and compete for prizes."
- CTA button: "Sign in" — `background: #534AB7`, `color: #fff`, `font-size: 13px`, `border-radius: 8px`, `padding: 11px 18px`. Matches existing `.gsp-account-cta`.

### 5. Practice Mode Indicator

Location: first-play card (`.fp`), shown beneath the hook line for under-18 users.

Visual: amber-bordered notice block.
- `background: rgba(239,159,39,0.1)`, `border: 0.5px solid rgba(239,159,39,0.4)`, `border-radius: 8px`, `padding: 8px 12px`
- Text (13px, system sans-serif, weight 400): "Practice mode — scores don't count toward prizes or the 18+ leaderboard."
- Color: `rgba(239,159,39,1)` (`--action-amber`) for the text

Do NOT show the Practice Mode indicator inside the HUD bar in Phase 2 (HUD modifications are deferred to Phase 4 leaderboard work).

---

## Interaction States

| Component | State | Behavior |
|-----------|-------|----------|
| Auth inputs | Focus | 2px solid `#534AB7` ring, `outline-offset: 0`, no default browser outline |
| Auth inputs | Error | `border-color: #f5a0a0`, keep focus ring |
| Auth inputs | Disabled | `opacity: 0.5`, `pointer-events: none` |
| Primary CTA button | Default | `--red` fill, `--brown` border 3px, box-shadow 4px 4px 0 `--brown` |
| Primary CTA button | Active/pressed | `opacity: 0.88` (matches existing `.fp-btn-play:active`) |
| Primary CTA button | Loading (Supabase call in flight) | Button text replaced with "···" (three middle dots), `pointer-events: none` |
| Primary CTA button | Disabled (checkbox unchecked) | `opacity: 0.4`, `pointer-events: none` |
| Age checkbox | Unchecked | Native checkbox; wrap in `<label>` for tap area |
| Age checkbox | Checked | Native checkbox (no custom styling in Phase 2) |
| Sign Out button | Active | `filter: brightness(1.15)` (matches existing) |
| "Forgot password?" link | Tap | Replaces form in place, no navigation |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — signup | "Create account" |
| Primary CTA — sign in | "Sign in" |
| Primary CTA — forgot password | "Send reset link" |
| Primary CTA — account save (v1: no save needed) | n/a |
| Age declaration checkbox label | "I am 18 or older" |
| Age declaration sub-copy | "Under 18? You can still play in practice mode — scores won't count toward prizes." |
| Practice Mode badge text | "Practice Mode" |
| Competitor badge text | "Competitor" |
| Signup hook (auth-status success) | "Account created — you're in!" |
| Sign in success (auth-status) | "Welcome back!" |
| Forgot password confirmation | "Reset link sent — check your email." |
| Empty state — Account tab (not logged in) | "Sign in to see your account and compete for prizes." |
| Error — email already in use | "That email is already registered. Try signing in instead." |
| Error — wrong password | "Wrong password. Try again or reset it below." |
| Error — email not found | "No account with that email. Create one instead." |
| Error — weak password | "Password must be at least 8 characters." |
| Error — network/Supabase failure | "Something went wrong. Check your connection and try again." |
| Guest nudge (existing, unchanged) | "Playing as guest — scores won't count toward the leaderboard" |
| Sign in / sign up prompt (existing, unchanged) | "Sign in / sign up to compete" |
| Sign Out button label | "Sign out" |
| Forgot password link | "Forgot password?" |
| Back to sign in link | "Back to sign in" |

Destructive actions in this phase:
- Sign Out: no confirmation dialog. Single tap on "Sign out" button executes immediately. This is recoverable (user can sign back in) so no confirmation is required. The ghost/danger color treatment (red border, red text) provides sufficient visual warning.

---

## Accessibility

- All form fields must have an associated `<label>` (visually hidden labels acceptable if placeholder provides sufficient context, but prefer visible labels on the signup form).
- The age checkbox `<label>` must wrap or be associated via `for`/`id` and must produce a tap target of minimum 44px height.
- `aria-live="polite"` on `#auth-status` span so screen readers announce success/error messages without requiring focus.
- `type="email"` and `type="password"` must have correct `autocomplete` values as specified above.
- Password field must not have `autocomplete="off"` — allow password managers.
- "Forgot password?" and "Back to sign in" are functional buttons (`<button type="button">`) not `<a>` tags, since they do not navigate to a URL.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — project is vanilla JS | not applicable |
| third-party | none | not applicable |

No component registries are used. All UI is hand-authored CSS + HTML matching existing prototype patterns.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

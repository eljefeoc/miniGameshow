---
phase: 01-phone-first-shell-pwa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prototypes/index.html
  - vercel.json
autonomous: true
requirements:
  - SHEL-02
  - SHEL-05

must_haves:
  truths:
    - "Visiting / on Vercel loads the new portrait home shell, not penguin-game.html"
    - "Home shell is portrait-first: vertically scrollable, shows event placeholder and a Play button"
    - "Tapping Play on the home shell navigates to /penguin-game.html"
    - "Home shell uses no build step — plain HTML/CSS/JS file served from prototypes/"
    - "All interactive elements on the home shell meet the 44×44px tap target minimum"
    - "Page layout does not break on orientation change between portrait and landscape"
  artifacts:
    - path: "prototypes/index.html"
      provides: "Portrait home shell — entry point for the app"
      contains: "Play"
    - path: "vercel.json"
      provides: "Updated route rewrite: / → index.html"
      contains: "index.html"
  key_links:
    - from: "prototypes/index.html"
      to: "/penguin-game.html"
      via: "Play button href or window.location.href"
      pattern: "penguin-game\\.html"
    - from: "vercel.json"
      to: "prototypes/index.html"
      via: "rewrites destination"
      pattern: "destination.*index\\.html"
---

<objective>
Create the portrait-oriented home shell (`prototypes/index.html`) that becomes the app entry point at `/`, and update `vercel.json` to route `/` to it instead of directly to the game.

Purpose: The game's discovery moment — someone taps a shared link and lands here before the game. Portrait-first, bold, event info front and center per D-01/D-02/D-03.
Output: New `prototypes/index.html`; updated `vercel.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-phone-first-shell-pwa/01-CONTEXT.md

<interfaces>
<!-- Existing CSS custom properties from prototypes/penguin-game.html — use these, do not introduce new colors -->
```css
:root {
  --teal:   #68C7C1;
  --yellow: #FACA78;
  --peach:  #F57F5B;
  --red:    #DD5341;
  --brown:  #794A3A;
  --cream:  #F5EDD6;
  --dark:   #2A1A0E;   /* = theme color #2A1A0E */
}
```

<!-- Current vercel.json (full file): -->
```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "prototypes",
  "rewrites": [
    { "source": "/", "destination": "/penguin-game.html" }
  ]
}
```

<!-- Fonts already loaded in penguin-game.html — mirror the same Google Fonts link in index.html -->
Titan One + Bubblegum Sans:
  https://fonts.googleapis.com/css2?family=Titan+One&family=Bubblegum+Sans&display=swap

<!-- GAME_BIBLE.md tone guidance (§3 The Feeling, §5 The World):
  Arctic world, Pixar warmth, playful + light, makes you smile in 5 seconds.
  Color palette: teal, yellow/cream, brown/dark. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prototypes/index.html — portrait home shell</name>
  <files>prototypes/index.html</files>
  <read_first>
    - .planning/phases/01-phone-first-shell-pwa/01-CONTEXT.md (D-01 through D-06, D-15, D-16, deferred section)
    - .planning/PROJECT.md (Core Value, tone)
    - prototypes/penguin-game.html lines 1–30 (viewport meta pattern to mirror)
    - GAME_BIBLE.md (§3 The Feeling, §5 The World — skim for tone)
  </read_first>
  <action>
Create `prototypes/index.html` as a new plain HTML file. No build step. File served directly from `prototypes/`. Follow all constraints below exactly.

**Document structure:**
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <title>miniGameshow</title>
  <!-- PWA manifest — created in Plan 03 -->
  <link rel="manifest" href="/manifest.json">
  <!-- Fonts (same as penguin-game.html) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" as="font" crossorigin href="https://fonts.gstatic.com/s/titanone/v13/mFTyWbofw6zc9NtnW43SuRwr.woff2">
  <link href="https://fonts.googleapis.com/css2?family=Titan+One&family=Bubblegum+Sans&display=swap" rel="stylesheet">
  <style>
    /* ... all CSS inline, no external stylesheet required ... */
  </style>
</head>
<body>
  <!-- shell content -->
  <script>
    /* ... navigation only ... */
  </script>
</body>
</html>
```

**CSS requirements (inline `<style>` block):**

```css
:root {
  --teal:   #68C7C1;
  --yellow: #FACA78;
  --peach:  #F57F5B;
  --red:    #DD5341;
  --brown:  #794A3A;
  --cream:  #F5EDD6;
  --dark:   #2A1A0E;
}

*, *::before, *::after {
  margin: 0; padding: 0; box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}

html, body {
  width: 100%; min-height: 100%;
  background: var(--dark);
  font-family: 'Bubblegum Sans', 'Trebuchet MS', sans-serif;
  color: var(--cream);
  overflow-x: hidden;   /* portrait scroll, no horizontal */
}

/* Full-height shell: vertically scrollable column */
.shell {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100dvh;
  padding: max(48px, env(safe-area-inset-top)) 24px max(32px, env(safe-area-inset-bottom));
  gap: 24px;
}

/* Show title */
.show-title {
  font-family: 'Titan One', 'Trebuchet MS', sans-serif;
  font-size: clamp(28px, 8vw, 48px);
  color: var(--yellow);
  text-align: center;
  text-shadow: 3px 3px 0 var(--dark);
  line-height: 1.1;
}

/* Event card — placeholder; Phase 3 wires real event data */
.event-card {
  width: 100%;
  max-width: 400px;
  background: var(--brown);
  border: 3px solid var(--yellow);
  border-radius: 20px;
  padding: 24px 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  box-shadow: 0 4px 0 var(--dark);
}

.event-label {
  font-size: 11px;
  letter-spacing: 0.09em;
  text-transform: uppercase;
  color: var(--yellow);
  opacity: 0.75;
}

.event-name {
  font-family: 'Titan One', 'Trebuchet MS', sans-serif;
  font-size: clamp(20px, 5vw, 28px);
  color: var(--cream);
}

.event-prize {
  font-size: 16px;
  color: var(--yellow);
}

/* Play button — D-11 minimum 44×44px; actual size much larger for thumb reach */
.btn-play {
  display: block;
  width: 100%;
  max-width: 400px;
  min-height: 64px;           /* well above 44px minimum */
  padding: 16px 24px;
  background: var(--teal);
  color: var(--dark);
  border: none;
  border-radius: 18px;
  font-family: 'Titan One', 'Trebuchet MS', sans-serif;
  font-size: clamp(20px, 5vw, 26px);
  text-align: center;
  text-decoration: none;
  cursor: pointer;
  box-shadow: 0 4px 0 #3A8A85, 0 6px 24px rgba(104,199,193,0.3);
  -webkit-tap-highlight-color: transparent;
  touch-action: manipulation;
  transition: transform 0.1s, box-shadow 0.1s;
  user-select: none;
}

.btn-play:active {
  transform: translateY(3px);
  box-shadow: 0 1px 0 #3A8A85, 0 2px 8px rgba(104,199,193,0.2);
}

/* Character art placeholder — will be replaced when icon crop is done in Plan 03 */
.char-art {
  font-size: 80px;
  text-align: center;
  line-height: 1;
  filter: drop-shadow(0 4px 8px rgba(0,0,0,0.4));
}

/* Tagline */
.tagline {
  font-size: clamp(14px, 3.5vw, 18px);
  color: var(--cream);
  opacity: 0.75;
  text-align: center;
  max-width: 320px;
}

/* Footer spacer */
.footer {
  font-size: 11px;
  color: var(--cream);
  opacity: 0.35;
  text-align: center;
}
```

**HTML body content:**
```html
<div class="shell">
  <h1 class="show-title">mini<br>Gameshow</h1>

  <div class="char-art">🐧</div>

  <p class="tagline">Tap Play. Five chances a day.<br>Beat everyone. Win prizes.</p>

  <div class="event-card" id="event-card">
    <div class="event-label">This week's event</div>
    <div class="event-name" id="event-name">Pengu Fisher</div>
    <div class="event-prize" id="event-prize">Play now — prizes coming soon</div>
  </div>

  <a href="/penguin-game.html" class="btn-play" id="btn-play" role="button">
    ▶ Play Now
  </a>

  <p class="footer">miniGameshow · No app needed · Free to play</p>
</div>
```

**JavaScript (inline `<script>` block):**
Keep minimal — just enhance the Play button with a fallback for non-Vercel local dev:

```javascript
// Resolve game path: works on Vercel (/ → index.html, /penguin-game.html served)
// and on local file:// or localhost (prototypes/index.html next to penguin-game.html)
const playBtn = document.getElementById('btn-play');
if (playBtn) {
  playBtn.addEventListener('click', function(e) {
    // href is already set to /penguin-game.html — browser handles navigation
    // This handler is only for future enhancement (e.g. pass event params)
  });
}
```

The `href="/penguin-game.html"` on the anchor handles navigation. No JS navigation needed.

**Do NOT include:**
- Event system data fetching (deferred to Phase 3)
- Leaderboard preview (deferred)
- Animated splash transitions (deferred per CONTEXT.md)
- Any external JS beyond fonts
  </action>
  <verify>
    <automated>grep -n "viewport-fit=cover\|btn-play\|penguin-game\.html\|min-height: 64px\|safe-area-inset" /Users/jeffreyhorn/Documents/MiniGameshow/miniGameshow/prototypes/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "viewport-fit=cover" prototypes/index.html` returns 1 match
    - `grep "penguin-game.html" prototypes/index.html` returns at least 1 match (the Play button href)
    - `grep "min-height: 64px" prototypes/index.html` returns 1 match (Play button tap target)
    - `grep "safe-area-inset-top\|safe-area-inset-bottom" prototypes/index.html` returns at least 2 matches (shell padding)
    - `grep "overflow-x: hidden" prototypes/index.html` returns 1 match (no horizontal scroll)
    - File has no `<script src=` tags other than inline — no external JS dependencies
    - File is valid HTML: `grep "<!DOCTYPE html>" prototypes/index.html` returns 1 match
  </acceptance_criteria>
  <done>Portrait home shell exists at `prototypes/index.html` with Play button linking to `/penguin-game.html`, safe-area insets, and no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Update vercel.json to route / to index.html</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (full file — 8 lines, must read before overwriting)
    - .planning/phases/01-phone-first-shell-pwa/01-CONTEXT.md (D-03)
  </read_first>
  <action>
Update the single rewrite rule in `vercel.json` to point `/` at the new home shell instead of directly at the game.

Current:
```json
{ "source": "/", "destination": "/penguin-game.html" }
```

Change to:
```json
{ "source": "/", "destination": "/index.html" }
```

The full updated `vercel.json` (all other fields unchanged):
```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "prototypes",
  "rewrites": [
    { "source": "/", "destination": "/index.html" }
  ]
}
```

No other changes. `buildCommand` and `outputDirectory` stay the same. Do NOT add `/penguin-game.html` as a rewrite target — it is served directly by Vercel as a static file (no rewrite needed for named paths).
  </action>
  <verify>
    <automated>grep -n "index.html\|penguin-game" /Users/jeffreyhorn/Documents/MiniGameshow/miniGameshow/vercel.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "destination" vercel.json` returns exactly `"destination": "/index.html"`
    - `grep "penguin-game.html" vercel.json` returns 0 matches (old route removed)
    - `cat vercel.json | python3 -c "import sys,json; json.load(sys.stdin); print('valid')"` prints `valid` (JSON is valid)
    - File has exactly 7 lines (same structure, single destination changed)
  </acceptance_criteria>
  <done>`vercel.json` routes `/` to `/index.html`; the game is still reachable at `/penguin-game.html` directly.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep "destination" vercel.json` — must contain `/index.html`, not `/penguin-game.html`
2. `ls prototypes/index.html` — file must exist
3. `grep "btn-play\|penguin-game.html\|safe-area-inset" prototypes/index.html | wc -l` — at least 4 matches
4. `grep "overflow-x" prototypes/index.html` — returns `overflow-x: hidden` (no horizontal scroll)
5. Spot-check: open `prototypes/index.html` in a browser at 375px width — no horizontal scrollbar, Play button visible, tapping it navigates to game.
</verification>

<success_criteria>
- `prototypes/index.html` exists with portrait layout, Play button (href `/penguin-game.html`), safe-area insets, no horizontal scroll
- `vercel.json` rewrites `/` to `/index.html`
- No new external JS dependencies in index.html
- All interactive elements ≥ 64px tall (well above 44px minimum)
</success_criteria>

<output>
After completion, create `.planning/phases/01-phone-first-shell-pwa/01-02-SUMMARY.md`
</output>
